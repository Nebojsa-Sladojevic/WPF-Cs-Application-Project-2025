<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:ClientApplication.CustomUI">
    <Style x:Key="ButtonDimensions" TargetType="Control">
        <Setter Property="Height" Value="30"/>
        <Setter Property="Margin" Value="4,0"/>
        <Setter Property="Padding" Value="6,0"/>
    </Style>
    <Style TargetType="StackPanel">
        <Setter Property="Background" Value="Transparent"/>
    </Style>
    <Style TargetType="Grid">
        <Setter Property="Background" Value="Transparent"/>
    </Style>
    <Style TargetType="Border">
        <!--Setter Property="CornerRadius" Value="5"/-->
    </Style>
    <Style TargetType="Button" BasedOn="{StaticResource ButtonDimensions}">
        <Setter Property="Background" Value="{StaticResource BackgroundLightColor}"/>
    </Style>
    <Style TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource TextColor}"/>
        <Setter Property="FontSize" Value="14"/>
    </Style>
    <Style TargetType="TextBox" BasedOn="{StaticResource ButtonDimensions}">
        <Setter Property="Background" Value="{StaticResource BackgroundColor}"/>
        <Setter Property="Foreground" Value="{StaticResource TextColor}"/>
    </Style>
    <!--Popup Placement="Bottom" IsOpen="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}}">
        <ScrollViewer x:Name="DropDownScrollViewer">
            <ItemsPresenter x:Name="ItemsPresenter" KeyboardNavigation.DirectionalNavigation="Contained"/>
        </ScrollViewer>
    </Popup-->
    <Style TargetType="Menu">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderBrush" Value="{StaticResource AccentColor}"/>
        <Setter Property="BorderThickness" Value="0"/>
    </Style>
    <Style TargetType="MenuItem" BasedOn="{StaticResource ButtonDimensions}">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderBrush" Value="{StaticResource AccentColor}"/>
        <Setter Property="BorderThickness" Value="1"/>
    </Style>
    <Style TargetType="RadioButton" BasedOn="{StaticResource ButtonDimensions}">
        <Setter Property="Background" Value="{StaticResource BackgroundLightColor}"/>
    </Style>
    <!--Style TargetType="CheckBox">
        <Setter Property="Background" Value="{StaticResource BackgroundLightColor}"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="BorderBrush" Value="{StaticResource AccentColor}"/>
        <Setter Property="BorderThickness" Value="1"/>
    </Style-->
    <Style x:Key="TitleText" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource TextColor}"/>
        <Setter Property="FontSize" Value="24"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
    </Style>
    <Style x:Key="NavigationPaneText" TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
        <Setter Property="Margin" Value="5"/>
        <Setter Property="FontSize" Value="14"/>
    </Style>
    <Style TargetType="local:NavigationPaneButton">
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Foreground" Value="{StaticResource TextColor}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:NavigationPaneButton">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                        <Path Style="{StaticResource NavPaneIcon}" Margin="5,0" VerticalAlignment="Stretch"/>
                        <TextBlock Style="{StaticResource NavigationPaneText}" FontWeight="Medium" VerticalAlignment="Stretch"
                                   Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Control}}"
                                   Text="{TemplateBinding Content}"/>
                    </StackPanel>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Foreground" Value="{StaticResource AccentHoverColor}"/>
                        </Trigger>
                        <Trigger Property="IsTogglePressed" Value="True">
                            <Setter Property="Foreground" Value="{StaticResource AccentHoverColor}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style TargetType="ComboBox">
        <!--Setter Property="Background" Value="{StaticResource BackgroundLightColor}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="BorderBrush" Value="{StaticResource AccentColor}"/-->
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBox">
                    <Button x:Name="Button" Background="{StaticResource BackgroundLightColor}">
                        <Grid x:Name="MainGrid" Background="Transparent">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="auto"/>
                            </Grid.ColumnDefinitions>
                            <ContentPresenter x:Name="ContentSite" Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                            <Path x:Name="Arrow" Grid.Column="1" Data="M0,0 L4,4 8,0 z" Margin="5,0,0,0"
                                  Fill="{StaticResource AccentColor}" VerticalAlignment="Center" HorizontalAlignment="Center" Stretch="Uniform"/>
                            <Popup x:Name="PART_Popup" Placement="Bottom" IsOpen="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}}">
                                <ScrollViewer>
                                    <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained"/>
                                </ScrollViewer>
                            </Popup>
                        </Grid>
                    </Button>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>